<div class="navbar navbar-expand-sm navbar-lewagon navbar-transparent <%= 'navbar-margin-top' if controller_name == 'registrations' || controller_name == 'sessions' %>">
    <%= link_to cards_path(query: "missed"), class: "navbar-brand" do %>
    <h1 class="nav-logo-long <%= 'nav-logo-grey' if controller_name == 'registrations' || controller_name == 'sessions' %>">neighbors.</h1>
      <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">

        <li class="nav-item d-flex" >
          <% if !user_signed_in? %>
            <%= link_to "Log In", new_user_session_path, class: "nav-link homepage-login" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link homepage-signup" %>
          <% else %>
            <%= link_to "Sign Out", destroy_user_session_path, class: "nav-link homepage-signup" %>
        </li>
        <% end %>
      </ul>
    </div>
  </div>

